import React from 'react';
import { ReflexContainer, ReflexElement, ReflexSplitter } from 'react-reflex';

import Viewer from './coponents/Viewer';

function App() {
  return (
    <div>
      <Viewer></Viewer>
    </div>
  );
}
export default App;
